# Стартовый скрипт этапа 2: заглушки, кавычки, ошибки

# заглушки с аргументами
ls
ls -la "my dir"
cd 'a b' c

# ошибки: неизвестная команда и незакрытая кавычка
foo bar
ls "oops

# пустые строки выше просто пропускаются
exit 3
